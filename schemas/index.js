const schema = require("./contacts");
const auth = require("./auth");
const schemaSubscription = require("./updateSubscription");
const verify = require("./verify");

module.exports = { schema, auth, schemaSubscription, verify };
